---
from: glebfarmon
created: 2024-06-10 18:24
tags:
- winwintravel/test-task
---

![[Logo (40 px).png]]
# Тестове завдання від glebfarmon

Тестове завдання - [github](https://github.com/glebfarmon/wwt-technical-task)
## Загально

Завдання виконане добре, всі функції працюють, як описано, молодець
Стилізація виконана добре і кінцевий додаток відповідає прототипу в Figma.

Однак, складність коду можна зменшити
Підхід до узагальнення дій в модальних вікнах креативний та добре виконаний

Присутні незначні синтаксичні помилки, ймовірно через незнайомство з новими технологіями

## Оцінки

%% Від 0 до 10 %%

Загальна оцінка: **8**
Зрозумілість коду: 2
Масштабованість коду: 7
Оптимізація: 9

## Базові перевірки

%% Так/Ні: ✅/⚠️/🚫 %%

**Чи виконанні всі умови по ТЗ?** 
✅, все виконанно і все працює добре

### Автоматичні перевірки

- `Чи збирається проєкт?` - ✅
- `Чи проходить перевірка лінтером?` - ✅
- `Чи немає помилок форматування?` - ✅
- `Чи проходить перевірка на типи?` - ✅

### Чи використанні всі необхідні технології?

#### Базові технології

- `git + conventional commits` - 🚫
- `react` - ✅
- `Chakra UI` - ✅
- `zustand` - ✅
- `i18n` - ✅
- `react-query`- ✅ 

## Що гарно

Використання compound паттерну заслуговує похвали

### Доповнення від розробника

В якості доповнення до завдання Ви реалізували зберігання фільтрів в URLSearchParams

Це виглядає зручно, молодець, що Ви це реалізували

Мені подобається те, що ви вирішили зберігати данні саме в URLSearchParams, а не в localStorage. Це гарне рішення для шерінгу данних


**Як це можна покращити?**

Можна замість зберігання в форматі 
`{\"state\"%3A{\"storedFilters\"%3A[{\"id\"%3A\"RULES_POLICIES_PAYMENT\"%2C\"type\"%3A\"OPTION\"%2C\"optionsIds\"%...`

Зберігати в форматі `filters[4].type=OR&filters[4].optionsIds[0]=spa&filters[4].optionsIds[1]=sauna`

Такий формат займає меньше символів та більш читаемий

## Недоліки

`useStore` - Це цікава ідея написати свій контроллер для модалок, тобто це стора для відриття/закриття модалок та збереження їх порядку

Весь цей функціонал вже є реалізованний в Chakra UI. 

Нам не потрібно писати кастомну логіку відкриття модалок коли ми можемо використовувати ту, яку нам надає бібліотека, яку вже точно протестували і яка точно працює

Одним з варіантів використання стори є рендер декількох модалок одна на одній

В цьому випадку ми можемо відрендерити модалку всередині модалки. Це звучить не дуже добре, але максимально просто, зрозуміло та точно буде працювати

Якщо обирати між тим, щоб можна було швидко зрозуміти код та почати працювати з ним та створенням переускладнених речей - краще обирати перше

---

**Дуже переускладенний код, його досить важко швидко зрозуміти. Це критично при роботі в команді, коли декілька розробників в майбутньому можуть редагувати твою фічу**

---

Немає відслідковування помилок в hashStorage, що може призвести до крашу застосунку якщо в URL будуть якісь помилки

---

Компоненти краще не зберігати в `index` файлах

---

Функції `addFilter` і `removeFilter` включають вкладені оновлення стану, які можуть бути складними для відстеження

## Удосконалення

Щоб в `t('filter:open')` не писати `filter:` можна в хук `useTranslation` прокинути `filter` в якості параметра, щоб використовувати `filter` namespace

---

Замість кастомного `generateId` можна підключити бібліотеку, це буде більш надійно

---

Замість збереження `storedFilters` та `filters` в одній сторі можна створити дві різних

### Незрозумілі моменти

### Nits

```tsx
if (existingFilterIndex !== -1) {
	...
}
```

Можна замінити на 

```tsx
if (existingFilterIndex === -1) return
...
```